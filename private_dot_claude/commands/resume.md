Load context from NOTES.md and get oriented quickly so you can start working immediately.

Steps:
1. Read NOTES.md if it exists (at the project root)
2. Run `git log --oneline -5` to see recent commit history
3. Run `git status` to see the current working state

Then summarize in 3–5 bullet points:
- What was being worked on last time
- What needs to be done next
- Any blockers, pending decisions, or open questions

Be concise. No need to repeat file contents verbatim — just orient the user so they can jump back in immediately.
