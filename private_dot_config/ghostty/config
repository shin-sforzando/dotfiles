# Font and appearance

font-family = "UDEV Gothic NF"
font-family = "Mononoki Nerd Font Mono"
font-family = "HackGen Console NF"
font-family = "Menlo"
font-size = 14
font-thicken = true
window-title-font-family = "UDEV Gothic NF"
macos-titlebar-style = tabs
macos-option-as-alt = true

clipboard-read = allow
clipboard-write = allow
clipboard-trim-trailing-spaces = true
copy-on-select = true

# Disable automatic shell integration injection
# We manually load it in .zshrc to ensure correct hook order

shell-integration = none

# Auto-install the xterm-ghostty terminfo on SSH remotes (falls back to
# xterm-256color + env vars if the remote has no `tic`). Only the ssh-* features
# are flipped on; the rest match Ghostty's defaults. The manually-sourced
# integration in .zshrc reads GHOSTTY_SHELL_FEATURES, which is set even with
# shell-integration = none, so this works without re-enabling auto-injection.
shell-integration-features = cursor,no-sudo,title,ssh-env,ssh-terminfo,path

window-padding-x = 8
window-padding-y = 16
window-padding-balance = true
window-save-state = always
window-new-tab-position = end

background-opacity = 0.75
background-opacity-cells = true

# unfocused-split-opacity = 0.5
# unfocused-split-fill = #000000

split-divider-color = #cccccc

# Notification

notify-on-command-finish = always
notify-on-command-finish-action = notify
notify-on-command-finish-after = 30s

# Working Directory Inheritance

window-inherit-working-directory = false
tab-inherit-working-directory = true
split-inherit-working-directory = true

quit-after-last-window-closed = true

macos-option-as-alt = left

# Newline for Coding Assistant

keybind = shift+enter=text:\n

# Toggle Quick Terminal

keybind = global:cmd+backquote=toggle_quick_terminal

# Quick Terminal

quick-terminal-position = center
